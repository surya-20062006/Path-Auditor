global:
  scrape_interval: 10s
  evaluation_interval: 10s

scrape_configs:
  - job_name: "decision_path_auditor_backend"
    metrics_path: "/metrics"
    static_configs:
      - targets: ["backend:8000"]
        labels:
          environment: "production"
          service: "ai_governance_platform"
